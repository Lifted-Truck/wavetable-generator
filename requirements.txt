# Runtime + build dependencies for wtfoundry.
# Pre-provisioned into .venv BEFORE the autonomous build so that network
# access can stay denied throughout the locked-down run.
numpy>=1.24
scipy>=1.10
soundfile>=0.12
pyyaml>=6.0
matplotlib>=3.7

# Dev / harness tooling
pytest>=7.4
ruff>=0.4

# Run 2 (MCP skin) — pre-provisioned now so run 2 needs no network either.
mcp>=1.0
